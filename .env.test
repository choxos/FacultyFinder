# Local Testing Environment Variables
# These are dummy values for testing FastAPI locally without database

# Database settings (dummy values for testing)
DB_HOST=localhost
DB_PORT=5432
DB_USER=test_user
DB_PASSWORD=test_password
DB_NAME=test_db

# Application settings
DEBUG=true
ENVIRONMENT=development
LOG_LEVEL=INFO

# Security (for testing only)
SECRET_KEY=local-testing-secret-key-change-in-production
